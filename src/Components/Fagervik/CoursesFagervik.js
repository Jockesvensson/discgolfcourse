import React from 'react'
import { Outlet } from 'react-router-dom';

function CoursesFagervik() {
    return (
        <div>
            <Outlet />
        </div>
    );
}

export default CoursesFagervik